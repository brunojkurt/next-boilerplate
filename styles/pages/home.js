import styled from 'styled-components'
import { Typography } from 'components/UI/elements'

export const WelcomeText = styled(Typography).attrs({
  color: 'secondary',
  variant: 'h1',
  align: 'center'
})``